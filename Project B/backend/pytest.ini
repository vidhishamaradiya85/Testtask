[pytest]
# Run from the backend/ directory: pytest tests/
testpaths = tests
# Show short test summary for failures; verbose individual test names
addopts = -v --tb=short
# Treat warnings as warnings, not errors (keeps output clean)
filterwarnings =
    ignore::DeprecationWarning
